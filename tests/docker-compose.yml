version: '3.8'
services:
  
  fuse-server-immunespace-test:
    build:
      context: .
      dockerfile: tests/Dockerfile
    command: ["-s", "-vv", "-x"] 
    depends_on:
      - fuse-server-immunespace



    
    
